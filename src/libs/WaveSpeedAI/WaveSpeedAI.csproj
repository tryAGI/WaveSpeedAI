<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
  </PropertyGroup>

  <PropertyGroup Label="Nuget">
    <Description>C# SDK for the WaveSpeed AI API -- multi-model generation platform with 700+ models for image, video, and audio generation.</Description>
    <PackageTags>wavespeed-ai;ai;image-generation;video-generation;api;client;sdk;dotnet;openapi</PackageTags>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="Microsoft.Extensions.AI" Version="10.5.1" />
  </ItemGroup>

</Project>
